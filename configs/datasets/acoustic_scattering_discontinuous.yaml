dataset:
  name: acoustic_scattering_discontinuous
  input_dir: data/raw/acoustic_scattering_discontinuous
  output_dir: data/processed/acoustic_scattering_discontinuous
  splits: [train, valid, test]
  dataset_key: data
  downsample_factor: 4
  max_trajectories: 1000
  random_seed: 0
  max_steps: 60
  input_steps: 1
  output_steps: 1
  fields: 2
  stride: 1
  cache: none
