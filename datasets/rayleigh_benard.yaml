dataset:
  name: rayleigh_benard
  input_dir: data/raw/rayleigh_benard
  output_dir: data/processed/rayleigh_benard
  splits: [train, valid, test]
  dataset_key: data
  downsample_factor: 4
  max_trajectories: 1000
  random_seed: 0
  max_steps: 60
  input_steps: 1
  output_steps: 1
  fields: 3
  stride: 1
  cache: memory
